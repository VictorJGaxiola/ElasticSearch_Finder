$('#elements p').remove();
<% @products.each do |product| %>
  var paragraph = "<p>name: ";
  paragraph += '<%= product.name %> description: <%= product.description %> ';
  var img = "<img src='<%= product.imagen_url(:medium) %>' />"
  $('#elements').append(paragraph + img);
<% end %>
